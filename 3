
from mlkit import Perceptron, linear

inputNames = ['x1', 'x2']

perceptron = Perceptron(
                inputNames=inputNames,
                weights=[0.0, 0.0],
                bias=0.0,
                activationMethod=linear)

perceptron.fit([0, 0, 0, 1], 460)
perceptron.printResult()
