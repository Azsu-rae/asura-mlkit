
from ml import Perceptron
from ml.utils import sigmoid
from ml.utils import binary

# p1 = Perceptron(["Weather", "Company", "Distance"], [3, 2, 2], -3, sigmoid)
# p2 = Perceptron(["Weather", "Company", "Distance"], [3, 2, 2], -3, binary)
#
# p1.printResult()
# p2.printResult()
