
= Perceptron

A perceptron is a neural network with a single neuron trained for binary classification. It has to work with a linearly separable dataset.

= Definiton

Same mathematical definition of a neuron. For a a perceptron with $n$ inputs:

$ y = sum_(i=0)^n w_i + b $
